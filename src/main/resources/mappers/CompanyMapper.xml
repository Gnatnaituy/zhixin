<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhixin.mapper.CompanyMapper">

    <insert id="save">
        INSERT INTO `company` (name, description, location, sort)
        VALUES (#{company.name},
                #{company.description},
                #{company.location},
                #{company.sort})
    </insert>

    <update id="update">
        UPDATE `company`
        SET name        = #{company.name},
            description = #{company.description},
            location    = #{company.location},
            sort        = #{company.sort},
            update_time = NOW()
        WHERE id = #{company.id}
    </update>

    <update id="delete">
        UPDATE `company`
        SET is_deleted = 1
        WHERE id = #{id}
    </update>

</mapper>