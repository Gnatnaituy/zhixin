<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhixin.mapper.ModuleMapper">

    <insert id="save">
        INSERT INTO `module` (type, sub_type, title, description, image, content)
        VALUES (#{module.type},
                #{module.subType},
                #{module.title},
                #{module.descriptio },
                #{module.image},
                #{module.content})
    </insert>

    <update id="update">
        UPDATE `module`
        SET type        = #{module.type},
            sub_type    = #{module.subType},
            title       = #{module.title},
            description = #{module.description},
            image       = #{module.image},
            content     = #{module.content},
            update_time = NOW()
        WHERE id = #{module.id}
    </update>

    <update id="delete">
        UPDATE `module`
        SET is_deleted = 1
        WHERE id = #{id}
    </update>

</mapper>